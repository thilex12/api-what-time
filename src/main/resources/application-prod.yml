spring:
  application:
    name: api-what-time
  datasource:
    driver-class-name: org.h2.Driver
    password: password
    url: 'jdbc:postgresql://localhost:XXX:whattimedb'
    username: sa

  logging:
    level:
      root: INFO
